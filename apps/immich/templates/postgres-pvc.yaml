apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-immich-postgresql-0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: hostpath-ssd-home
