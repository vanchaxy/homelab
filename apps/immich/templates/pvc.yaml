apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photo-library
spec:
  storageClassName: hostpath-ssd-home
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi