apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photo-library
spec:
  storageClassName: longhorn-ssd-retain
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi