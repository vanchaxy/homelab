apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photo-library
spec:
  storageClassName: hostpath-ssd-home
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi