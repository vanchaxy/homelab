pocket-id:
  controllers:
    main:
      containers:
        main:
          image:
            repository: ghcr.io/pocket-id/pocket-id
            tag: v2.16.0@sha256:9366436f3fd21619ed7e5709fa0acac88130f73414ec8ee1caf768fc487111ea
          env:
            APP_URL: https://id.ivanchenko.io
            TRUST_PROXY: true
          envFrom:
            - secretRef:
                name: pocket-id
  service:
    main:
      controller: main
      type: ClusterIP
      ports:
        http:
          port: 1411

  route:
    main:
      hostnames:
        - id.ivanchenko.io
      parentRefs:
        - name: internal-gw
          namespace: envoy-gateway
          sectionName: https

  persistence:
    config:
      enabled: true
      suffix: config
      storageClass: longhorn-ssd
      size: 1Gi
      accessMode: ReadWriteOnce
      advancedMounts:
        main:
          main:
            - path: /app/data

helpers:
  infisicalSecrets:
    - name: pocket-id
      infisical:
        path: "/pocket-id"
  backups:
    - enabled: true
      pvcName: pocket-id-config
      resticRepoSuffix: pocket-id
      moverSecurityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
