apiVersion: operator.victoriametrics.com/v1beta1
kind: VMAlertmanagerConfig
metadata:
  name: alerting-routing
  namespace: telemetry
spec:
  route:
    receiver: telegram
    group_by: [alertname]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: healthchecks
        matchers:
          - alertname="Watchdog"
        group_wait: 0s
        group_interval: 1m
        repeat_interval: 5m
      - receiver: blackhole
        matchers:
          - alertname="InfoInhibitor"
  receivers:
    - name: telegram
      telegram_configs:
        - bot_token:
            name: alertmanager-secrets
            key: TELEGRAM_BOTTOKEN
          chat_id: 236266523
          parse_mode: MarkdownV2
          send_resolved: true
          disable_notifications: false
          # MarkdownV2 needs every reserved char escaped in dynamic text or
          # Telegram 400s and the alert is never delivered. $esc escapes body
          # text; URLs only need ) and \ escaped inside the (...) link target.
          message: |-
            {{- if eq .Status "firing" -}}
              {{- if eq .CommonLabels.severity "critical" }}🔥 *CRITICAL*
              {{- else if eq .CommonLabels.severity "warning" }}⚠️ *WARNING*
              {{- else }}ℹ️ *FIRING*{{ end }} · {{ .Alerts.Firing | len }} firing
            {{- else -}}
              ✅ *RESOLVED* · {{ .Alerts.Resolved | len }} resolved
            {{- end }}
            {{ range .Alerts }}
            {{- $esc := "[_*\\[\\]()~`>#+\\-=|{}.!\\\\]" -}}
            {{- if eq .Status "resolved" -}}
              ✅ *{{ reReplaceAll $esc "\\$0" .Labels.alertname }}*{{ with .Labels.namespace }} · `{{ . }}`{{ end }}
            {{ else -}}
              {{ if eq .Labels.severity "critical" }}🔥{{ else if eq .Labels.severity "warning" }}⚠️{{ else }}ℹ️{{ end }} *{{ reReplaceAll $esc "\\$0" .Labels.alertname }}*
              {{- with .Annotations.summary }}
            {{ reReplaceAll $esc "\\$0" . }}{{ end }}
              {{- with .Annotations.description }}
            {{ reReplaceAll $esc "\\$0" . }}{{ end }}
            ```
            {{ range .Labels.SortedPairs }}{{ if not (match "^(alertname|alertgroup|severity|condition|prometheus|endpoint|instance|job|service)$" .Name) }}{{ .Name }} = {{ .Value }}
            {{ end }}{{ end -}}
            ```
              {{- if or .GeneratorURL .Annotations.runbook_url }}
            {{ with .GeneratorURL }}🔗 [source]({{ reReplaceAll "[)\\\\]" "\\$0" . }}){{ end }}{{ with .Annotations.runbook_url }} · 📖 [runbook]({{ reReplaceAll "[)\\\\]" "\\$0" . }}){{ end }}
              {{- end }}
            {{ end -}}
            {{ end }}
    - name: healthchecks
      webhook_configs:
        - url_secret:
            name: alertmanager-secrets
            key: HEALTHCHECKS_URL
          send_resolved: false
    - name: blackhole
  inhibit_rules:
    - source_matchers: ['severity="critical"']
      target_matchers: ['severity=~"warning|info"']
      equal: [namespace, alertname]
    - source_matchers: ['severity="warning"']
      target_matchers: ['severity="info"']
      equal: [namespace, alertname]
    - source_matchers: ['alertname="InfoInhibitor"']
      target_matchers: ['severity="info"']
      equal: [namespace]
