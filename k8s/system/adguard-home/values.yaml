adguard-home:
  controllers:
    main:
      containers:
        main:
          image:
            repository: adguard/adguardhome
            tag: v0.107.70

  service:
    main:
      controller: main
      type: LoadBalancer
      loadBalancerIP: 192.168.50.250
      ports:
        http:
          port: 3000
        dns-tcp:
          port: 53
        dns-udp:
          protocol: UDP
          port: 53
        dns-over-tls:
          port: 853
        dns-over-quic:
          protocol: UDP
          port: 784

  route:
    main:
      hostnames:
        - adguard.ivanchenko.io
      parentRefs:
        - name: internal-gw
          namespace: envoy-gateway
          sectionName: https
      rules:
        - backendRefs:
            - identifier: main
              port: 3000

  persistence:
    config:
      enabled: true
      suffix: config
      storageClass: longhorn-ssd
      size: 1Gi
      accessMode: ReadWriteOnce
      advancedMounts:
        main:
          main:
            - path: /opt/adguardhome/conf
              subPath: conf
            - path: /opt/adguardhome/work
              subPath: work
