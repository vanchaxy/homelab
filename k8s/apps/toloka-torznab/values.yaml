toloka-torznab:
  controllers:
    main:
      pod:
        imagePullSecrets:
          - name: ghcr-login-secret
      containers:
        main:
          image:
            repository: ghcr.io/vanchaxy/toloka-indexer
            tag: sha-5939c0d
          envFrom:
            - secretRef:
                name: toloka-torznab-secret
    electric:
      containers:
        main:
          image:
            repository: electricsql/electric
            tag: 1.7.6@sha256:4b0746aff922a3b05c631a71623636d5767792a9362f20a9710998781feb5436
          env:
            ELECTRIC_INSECURE: 'true'
            ELECTRIC_MANUAL_TABLE_PUBLISHING: 'true'
            ELECTRIC_STORAGE_DIR: '/persistent'
            DATABASE_URL:
              valueFrom:
                secretKeyRef:
                  name: toloka-torznab-secret
                  key: ELECTRIC_DATABASE_URL

  service:
    main:
      controller: main
      type: ClusterIP
      ports:
        http:
          port: 8000
    electric:
      controller: electric
      type: ClusterIP
      ports:
        http:
          port: 3000

  route:
    main:
      hostnames:
        - toloka-torznab-api.ivanchenko.io
      parentRefs:
        - name: internal-gw
          namespace: envoy-gateway
          sectionName: https
      rules:
        - backendRefs:
            - identifier: main
    electric:
      hostnames:
        - toloka-torznab-electric.ivanchenko.io
      parentRefs:
        - name: internal-gw
          namespace: envoy-gateway
          sectionName: https
      rules:
        - backendRefs:
            - identifier: electric

  persistence:
    electric:
      enabled: true
      suffix: electric
      storageClass: longhorn-ssd
      size: 5Gi
      accessMode: ReadWriteOnce
      advancedMounts:
        electric:
          main:
            - path: /persistent

helpers:
  ghcrSecret:
    enabled: true
  infisicalSecrets:
    - name: toloka-torznab-secret
      type: kubernetes.io/basic-auth
      infisical:
        path: "/toloka-torznab"
  backups:
    - pvcName: toloka-torznab-postgres-1
      resticRepoSuffix: toloka-torznab/postgres
      moverSecurityContext:
        fsGroup: 26
        runAsGroup: 26
        runAsNonRoot: true
        runAsUser: 26
    - pvcName: toloka-torznab-electric
      resticRepoSuffix: toloka-torznab/electric
