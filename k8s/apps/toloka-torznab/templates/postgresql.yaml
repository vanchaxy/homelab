apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: toloka-torznab-postgres
spec:
  imageName: 'ghcr.io/cloudnative-pg/postgresql:18.3-minimal-trixie'
  instances: 1
  bootstrap:
    initdb:
      database: toloka
      owner: toloka
      secret:
        name: toloka-torznab-secret
  postgresql:
    parameters:
      wal_level: logical
      max_wal_senders: "10"
      max_replication_slots: "10"
      # Cap how much WAL an inactive logical slot may pin. If Electric stops
      # advancing, Postgres invalidates the slot instead of filling the PVC.
      max_slot_wal_keep_size: "8GB"
  managed:
    roles:
      - name: toloka
        ensure: present
        login: true
        createrole: true
        replication: true
        passwordSecret:
          name: toloka-torznab-secret
  storage:
    pvcTemplate:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 50Gi
      storageClassName: longhorn-ssd
