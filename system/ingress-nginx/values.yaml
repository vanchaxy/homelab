ingress-nginx:
  controller:
    ingressClassResource:
      default: true
    admissionWebhooks:
      timeoutSeconds: 30
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
